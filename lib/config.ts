/**
 * Single source of truth for the product name. Referenced by the manifest,
 * metadata, email templates and the IndexedDB database prefix, so renaming the
 * app means editing this file and nothing else.
 */
export const APP_NAME = 'Tend';
export const APP_TAGLINE = 'Look after what needs doing.';
export const APP_DB_PREFIX = 'tend';
